<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="gas_dialog" parent="Theme.AppCompat.Light.Dialog">
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowSoftInputMode">adjustPan</item>
        <item name="android:windowContentOverlay">@null</item>

    </style>
    <!--<item name="android:windowAnimationStyle">@style/gas_dialog_anim</item>-->

    <style name="gas_dialog_anim" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/gas_anim_show</item>
        <item name="android:windowExitAnimation">@anim/gas_anim_dismiss</item>
    </style>

    <style name="gas_dialog_divder_line">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.5dp</item>
        <item name="android:background">@color/cdedede</item>
    </style>

    <style name="gas_dialog_divder_line_vertical">
        <item name="android:layout_width">0.5dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/cdedede</item>
    </style>


</resources>